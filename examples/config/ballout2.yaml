consul:
  token: 
election:
  enabled:
    - my_service
  services:
    my_service:
      id: my_service2
      key: service/my_service
      token: 
      serviceChecks:
        - service:election2
      primaryTag: primary
      onPromote: '/usr/bin/printf promote'
      onDemote: '/usr/bin/printf demote'
