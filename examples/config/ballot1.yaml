consul:
  token: 
election:
  enabled:
    - my-service
  services:
    my_service:
      id: my-service1
      key: service/my-service
      token: 
      serviceChecks:
        - service:election1
      primaryTag: primary
      onPromote: '/usr/bin/printf promote'
      onDemote: '/usr/bin/printf demote'
